window.addEventListener('load', () => {
    var swiper = new Swiper(".mySwiper", {
        pagination: {
            clicable: true,
            el: ".swiper-pagination",
            dynamicBullets: true,
        },
    });
})
